• Findings

  - /D:/VATSAL/BankStatementAnalyzer/main_gui.py:652: Review refresh loads every uncategorized transaction into a QTableWidget at once. This will become
    sluggish with large datasets; add pagination, search/filtering, or lazy table models.

  - /D:/VATSAL/BankStatementAnalyzer/analyzer/import_manager.py:68: Inserts transactions one row at a time. For larger files, executemany() or chunked bulk
    inserts would be cleaner and faster.

  - /D:/VATSAL/BankStatementAnalyzer/analyzer/base_configurable_parser.py:34: Parser reads entire Excel files into memory and loops with iterrows(), which is
    slow for large statements. Prefer vectorized cleanup or chunk-oriented parsing where possible.

  - /D:/VATSAL/BankStatementAnalyzer/analyzer/export.py:66: Export uses SELECT * for bank sheets. This couples output to schema order and future columns;
    explicit columns are more maintainable.